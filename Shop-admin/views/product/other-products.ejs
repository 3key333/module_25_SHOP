<div class="other-product">

    <div class="other-product-title">
        <p><%= otherProduct.title %></p>
    </div>

    <div class="mark-to-add-to-similar">
        <label for="<%= otherProduct.id %>">Add to similar</label>
        <input id="<%= otherProduct.id %>" type="checkbox" value="<%= otherProduct.id %>" name="otherProductsToSimilar">
    </div>
    
</div>